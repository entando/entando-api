description: OK
content:
  application/json:
    schema:
      allOf:
        - $ref: ../../../schemas/restResponse/restResponseMapMetadata.yaml
        - properties:
            payload:
              $ref: ../../../schemas/pages/page.yaml