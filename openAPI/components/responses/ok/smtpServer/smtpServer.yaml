description: OK
content:
  application/json:
    schema:
      allOf:
        - $ref: ../../../schemas/restResponse/restResponse.yaml
        - properties:
            payload:
              $ref: ../../../schemas/smtpServer/smtpServer.yaml